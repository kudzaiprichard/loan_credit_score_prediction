# Core
pandas>=2.0
numpy>=1.24
scikit-learn>=1.4      # needs TargetEncoder + HistGB class_weight
joblib>=1.3

# Gradient boosting zoo
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2

# Imbalance utilities (optional alternative to class weights)
imbalanced-learn>=0.12

# Viz / EDA
matplotlib>=3.7
seaborn>=0.13

# Monitoring + app (idea 8)
evidently>=0.4
streamlit>=1.30

# Notebooks
jupyter
ipykernel

# Tests
pytest>=7.0

# Optional upgrades (not required — code degrades gracefully without them):
#   hdbscan        # density-based segmentation (src/segmentation.py)
#   umap-learn     # nicer 2-D cluster viz
#   pyod           # richer anomaly-detector ensemble (src/anomaly.py)
#   dice-ml        # optimisation-based recourse / counterfactuals (src/recourse.py)

# NOTE: shap is intentionally omitted — it conflicts with NumPy>=2.4 in this
# environment (numba). Explainability uses permutation importance + occlusion
# reason codes instead (see src/explain.py). To enable SHAP, pin numpy<=2.3.
